version: '3.7'

services:
  db:
    image: postgres:11
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: "smart-ride"
      POSTGRES_USER: "myuser"
      POSTGRES_PASSWORD: "mypassword"
